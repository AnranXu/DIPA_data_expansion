library(rstatix)
library(dplyr)
library(ggplot2)
library(ggpubr)
library(multcompView)
library(multcomp)
library(ez)
library(car)
data <- read.csv(file = './for_anova (mycat).csv')
data <- data %>% convert_as_factor(ID,category,informativeness, reason)
#homogneity <- data %>% group_by(category) %>% levene_test(importance ~ category)
#print(homogneity)ge
anova <- aov(importance ~ reason*informativeness, data = data)
sesummary(anova)
